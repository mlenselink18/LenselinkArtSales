@model List<ProductImageListView>
@{
    ViewBag.Title = "Product Manager";
}

<h1>@ViewBag.Title</h1>
<div class="row">
    <div class="col-sm-10">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th></th>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Active</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var p in Model)
                {
                    <tr>
                        <td><a asp-action="Update" asp-route-id="@p.product.Id">
                               <img class="thumbnail" alt="@p.FirstImageAlt" src="@Url.Content("~"+@p.FirstImageThumbnail)"/>
                            </a>
                        </td>
                        <td>@p.product.Title</td>
                        <td>@p.product.Description</td>
                        <td>@p.product.Active</td>
                        <td>
                            <a asp-action="Update"
                               asp-route-id="@p.product.Id">Update</a>
                        </td>
                        <td>
                            <a asp-action="Delete"
                               asp-route-id="@p.product.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        @{ 
            string userMessage = TempData?["UserMessage"]?.ToString() ?? ""; 
        }
        @if (userMessage != "") {
            <div class="text-success">@userMessage</div>
        }
        <a asp-action="Add" class="btn btn-primary">Add New Product</a>
    </div>
</div>